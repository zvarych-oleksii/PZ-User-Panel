<Window x:Class="WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login Form" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Username:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="5"/>
        <TextBox x:Name="txtUsername" Grid.Row="0" Margin="10"/>

        <Label Content="Password:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="5" Grid.Row="1"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="1" Margin="10"/>

        <Button Content="Login" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10" Click="Login_Click"/>
        <TextBlock x:Name="txtResult" Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10"/>
    </Grid>
</Window>